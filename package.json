{
  "name": "ctf-collection",
  "version": "latest",
  "description": "CTF Submission Collection Web App",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "start": "npm run start --workspaces",
    "dev": "npm run dev --workspace=server & npm run start --workspace=client",
    "install-all": "npm install && npm install --workspace=server && npm install --workspace=client"
  }
}
